<form action="@Url.Action("AddTask","Home",new {})" method="post">
    <div>
        @Html.Label("Name", "任务名称", new { })
        @Html.TextBox("Name", string.Empty, null, new { })
    </div>
    <div>
        @Html.Label("Content", "任务内容", new { })
        @Html.TextBox("Content", string.Empty, null, new { })
    </div>
    <div>
        <button type="submit">
            保存
        </button>
    </div>
</form>