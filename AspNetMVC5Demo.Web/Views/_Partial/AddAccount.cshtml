<form action="@Url.Action("AddAccount","Home",new {})" method="post">
    <div>
        @Html.Label("Name", "你的大名", new { })
        @Html.TextBox("Name", string.Empty, null, new { })
    </div>
    <div>
        <button type="submit">
            保存
        </button>
    </div>
</form>